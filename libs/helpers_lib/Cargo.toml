[package]
name = "helpers_lib"
version = "0.1.0"
edition = "2018"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
serde_json = "1.0"
config_lib = { path = "../config_lib" }
prints_lib = { path = "../prints_lib" }