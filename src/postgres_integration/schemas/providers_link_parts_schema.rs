table! {
    providers_link_parts (id) {
        id -> Integer,
        provider_kind -> Text,
        link_part -> Text,
    }
}

table! {
    arxiv_link_parts (id) {
        id -> Integer,
        provider_kind -> Text,
        link_part -> Text,
    }
}

table! {
    biorxiv_link_parts (id) {
        id -> Integer,
        provider_kind -> Text,
        link_part -> Text,
    }
}

table! {
    github_link_parts (id) {
        id -> Integer,
        provider_kind -> Text,
        link_part -> Text,
    }
}

table! {
    habr_link_parts (id) {
        id -> Integer,
        provider_kind -> Text,
        link_part -> Text,
    }
}

table! {
    medrxiv_link_parts (id) {
        id -> Integer,
        provider_kind -> Text,
        link_part -> Text,
    }
}

table! {
    reddit_link_parts (id) {
        id -> Integer,
        provider_kind -> Text,
        link_part -> Text,
    }
}

table! {
    twitter_link_parts (id) {
        id -> Integer,
        provider_kind -> Text,
        link_part -> Text,
    }
}
